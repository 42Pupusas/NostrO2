//! `nostro2-benchmarks` is a harness crate, not a library. The interesting
//! code is under `benches/`; this file exists because Cargo requires every
//! package to have at least one library or binary target before it can host
//! `[[bench]]` entries.
